#include "main.h"

/**
 * _strspn - gets the length of the prefix substring
 * @s: parameter that points to a string
 * @accept: prefix string
 * Return: returns the number of prefix
 */

unsigned int _strspn(char *s, char *accept)
{
	
