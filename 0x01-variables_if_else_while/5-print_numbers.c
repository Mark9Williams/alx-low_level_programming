#include <stdio.h>
/**
 * main - This is where the program starts
 * Return: returns 0
 */
/* Program that prints all single digit numbers of base 10 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	return (0);
}
