#include "main.h"

/**
 * add - add two integers
 * Return: returns the result
 * @a: It a parameter that stores the first argument
 * @b: It is a prameter that stores the second argument
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
